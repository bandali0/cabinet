<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.afollestad.cabinet">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_SUPERUSER" />
    <uses-permission android:name="com.android.vending.BILLING" />

    <uses-sdk tools:node="replace" android:minSdkVersion="16" android:targetSdkVersion="19" />

    <application
        android:name=".App"
        android:icon="@drawable/ic_launcher"
        android:logo="@drawable/ic_file_ab"
        android:label="@string/app_name"
        android:theme="@style/Theme.Cabinet">
        <activity
            android:name=".ui.DrawerActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".services.NetworkService"
            android:exported="false"
            android:label="@string/network_service_label" />
    </application>

</manifest>
